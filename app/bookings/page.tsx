// TODO: list/calendar of is_bookable assets, new booking form (calls
// bookResource(), shows OverlapModal on block), cancel/reschedule while Upcoming.
export default function BookingsPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-4">Resource Booking</h1>
      <p className="text-ink-soft">TODO — see PRD 5.6. Core logic already in lib/business-logic.ts.</p>
    </div>
  );
}
