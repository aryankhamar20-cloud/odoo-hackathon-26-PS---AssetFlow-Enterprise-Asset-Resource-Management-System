// TODO: raise request form, ApprovalStepper (Pending -> Approved/Rejected ->
// Technician Assigned -> In Progress -> Resolved), approve/resolve buttons
// wired to approveMaintenance()/resolveMaintenance().
export default function MaintenancePage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-4">Maintenance</h1>
      <p className="text-ink-soft">TODO — see PRD 5.7. Core logic already in lib/business-logic.ts.</p>
    </div>
  );
}
