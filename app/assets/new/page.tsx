// TODO: form for Name, Category, Serial Number, Acquisition Date/Cost,
// Condition, Location, photo (or URL fallback), is_bookable.
// On submit: call generateAssetTag() from lib/business-logic.ts before insert.
export default function NewAssetPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-4">Register Asset</h1>
      <p className="text-ink-soft">TODO: form — see PRD 5.4.</p>
    </div>
  );
}
