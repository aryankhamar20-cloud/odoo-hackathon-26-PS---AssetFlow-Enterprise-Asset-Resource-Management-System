// TODO: allocate to Employee/Department, transfer request list + approve
// buttons (Asset Manager / Dept Head only), return flow.
export default function AllocationsPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold mb-4">Allocations & Transfers</h1>
      <p className="text-ink-soft">TODO — see PRD 5.5. Core logic already in lib/business-logic.ts.</p>
    </div>
  );
}
